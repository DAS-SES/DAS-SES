# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Dotazník</title>
  <script>
    // sem vlož odkazy na své Survio dotazníky:
    const EXP_URL = "https://www.survio.com/survey/d/J9H0D9Y4L5E9L7Z0M"; 
    const CTRL_URL = "https://www.survio.com/survey/d/R8G6S5N5D9E5Z1K7K";     

    // vytvoření/čtení cookie
    function setCookie(name, value, days) {
      const d = new Date();
      d.setTime(d.getTime() + (days*24*60*60*1000));
      let expires = "expires=" + d.toUTCString();
      document.cookie = name + "=" + value + ";" + expires + ";path=/";
    }

    function getCookie(name) {
      let cname = name + "=";
      let decoded = decodeURIComponent(document.cookie);
      let parts = decoded.split(';');
      for (let p of parts) {
        let c = p.trim();
        if (c.indexOf(cname) === 0) {
          return c.substring(cname.length, c.length);
        }
      }
      return "";
    }

    window.onload = function() {
      // zkus přečíst cookie
      let group = getCookie("group");

      if (!group) {
        // pokud není přiřazeno → přiřadíme náhodně
        group = Math.random() < 0.5 ? "exp" : "ctrl";
        setCookie("group", group, 30);   // cookie platná 30 dní
      }

      // přesměrování podle skupiny
      if (group === "exp") {
        window.location.href = EXP_URL;
      } else {
        window.location.href = CTRL_URL;
      }
    };
  </script>
</head>
<body>
  Probíhá přesměrování na dotazník…
</body>
</html>
